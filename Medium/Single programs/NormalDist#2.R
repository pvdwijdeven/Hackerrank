cat(sprintf("%.2f",100*pnorm(80, mean=70, sd=10, lower.tail=FALSE) ))
cat(sprintf("\n%.2f",100*pnorm(60, mean=70, sd=10, lower.tail=FALSE) ))
cat(sprintf("\n%.2f",100*pnorm(60, mean=70, sd=10, lower.tail=TRUE) ))
